name: Org ADR Auto Sync

on:
  schedule:
    - cron: "15 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read # required to inspect ADR mirror files
  pull-requests: read # required to inspect existing sync pull requests

concurrency:
  group: org-adr-auto-sync-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync:
    name: org ADR auto-sync
    permissions:
      contents: read # required by the reusable to inspect ADR mirror files
      pull-requests: read # required by the reusable to inspect sync PRs
    uses: NWarila/.github/.github/workflows/reusable-org-adr-auto-sync.yaml@16fc66816dea5a593c0d5fbb0574ce3b54b46991
    with:
      source_repo: NWarila/.github
      source_ref: main
      branch_name: sync/org-adrs
      pr_title: "[sync] refresh org ADR mirrors"
    secrets:
      sync_token: ${{ secrets.ORG_ADR_SYNC_TOKEN }}
