name: Org ADR Auto Sync

on:
  schedule:
    - cron: "15 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: org-adr-auto-sync-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync:
    name: org ADR auto-sync
    permissions:
      contents: read
      pull-requests: read
    uses: NWarila/.github/.github/workflows/reusable-org-adr-auto-sync.yaml@f25a0e36f0591687862ff78f9f7b9d94fe577bc7
    with:
      source_repo: NWarila/.github
      source_ref: main
      branch_name: sync/org-adrs
      pr_title: "[sync] refresh org ADR mirrors"
    secrets:
      sync_token: ${{ secrets.ORG_ADR_SYNC_TOKEN }}
