name: PR Validation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: NWarila/terraform-runner-template/.github/workflows/reusable-terraform-validation.yaml@v1
    with:
      terraform_version: "1.15.1"
      template_ref: v1
      mode: runner
      run_contract_check: true
      lint_advisory: false
      framework_repo: NWarila/terraform-framework-template
      framework_ref: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
      overlay_paths: |
        terraform/public => terraform/repos/public
        terraform/private => terraform/repos/private
        tests/fixtures => terraform/fixtures/runtime
